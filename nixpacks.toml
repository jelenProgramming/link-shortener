# Railway / Nixpacks build config for this Laravel API
providers = ["php"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan config:cache && php artisan route:cache && php artisan migrate --force --no-interaction; php -S 0.0.0.0:$PORT -t public"
